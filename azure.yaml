# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: techtrain-handson-azure-development
metadata:
  template: techtrain-handson-azure-development@0.0.1-beta
services:
  web:
    project: ./src/web
    dist: build
    language: js
    host: appservice
  api:
    project: ./src/api
    language: csharp
    host: appservice
  backend:
    project: ./src/backend
    language: csharp
    host: function
